/*
*  Include any modules you're testing here. The srcScripts is mapped to
*  ../../src/javascripts/. For example, if you have a module named "calc",
*  the line below might look like this:
*    define(['jasmine', 'srcScripts/calc'], function (jasmine, calc) {
*/
define(['jasmine'], function (jasmine) {
  /*
  *  Describe tests here. Following the example, you might write a description
  *  like this:
  *
  *    describe('calc', function () {
  *      it('adds numbers', function () {
  *        expect(calc.add(1, 1)).toBe(2);
  *      });
  *    });
  */
});